spring:
  jpa:
    hibernate:
      ddl-auto: create
      hibernate:
    show-sql: true

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sandwich_sql
    username: root
    password: Java7450

    properties:
      hibernate:
        format_sql: true

