spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sandwich_db
    username: root
    password: Java7450
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

